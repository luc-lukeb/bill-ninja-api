<h1>Listing items</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Price</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @items.each do |item| %>
  <tr>
    <td><%= item.name %></td>
    <td><%= item.price %></td>
    <td><%= link_to 'Show', bill_item_path(@bill, item) %></td>
    <td><%= link_to 'Edit', edit_bill_item_path(@bill, item) %></td>
    <td><%= link_to 'Destroy', bill_item_path(@bill, item), method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Item', new_bill_item_path %>
